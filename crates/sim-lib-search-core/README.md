# sim-lib-search-core

An open provider-neutral vocabulary for queries, observations, provider claims,
pages, notices, alias evidence, rank contributions, runs, verified citations,
and research bundles. Provider wire codecs are pure object-safe syntax adapters.
