[package]
name = "sim-cookbook"
version = "0.1.3"
edition = "2024"
license = "MPL-2.0"
description = "Cookbook engine for SIM: runnable, crate-local tutorial recipes."
readme = "README.md"
publish = true
repository = "https://github.com/sim-nest/sim-foundation"
homepage = "https://github.com/sim-nest/sim-foundation"

[dependencies]
sim-config = { version = "0.1.1", path = "../sim-config" }
sim-kernel = { version = "0.1.0" }
sim-value = { version = "0.1.2", path = "../sim-value" }

[lints]
workspace = true
