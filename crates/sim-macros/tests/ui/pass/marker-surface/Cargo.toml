[package]
name = "sim-macros-pass-marker-surface"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"
publish = false

[workspace]
members = []

[dependencies]
sim = { package = "sim-nest", version = "0.2.0", default-features = false, features = ["core", "shape", "proc-macros"] }
