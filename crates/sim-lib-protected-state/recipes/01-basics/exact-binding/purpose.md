# Exact protected-state binding

Demonstrates the descriptor for caller-owned opaque bytes protected with an
explicit purpose, audience, subject, context digest, expiry, retained key ring,
nonce source, wall clock, and optional Table-backed single-use claim.
