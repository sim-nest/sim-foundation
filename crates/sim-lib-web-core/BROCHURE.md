# Evidence that keeps its source

`sim-lib-web-core` separates retrieved bytes from normalized text and gives each
its own canonical identity. A quote is usable only when its Unicode-scalar range
and exact text still match that immutable representation. Complete typed policy
receipts make missing authorization a denial, never an ambient default.
