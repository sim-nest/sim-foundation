# sim-lib-web-core

Pure records for raw captures, normalized representations, Unicode-scalar
selectors, and complete fail-closed web policy receipts. It performs no I/O,
HTML parsing, provider adaptation, ranking, or office evidence work.
