[package]
name = "sim-conformance-core"
version = "0.2.0"
edition.workspace = true
license.workspace = true
description = "Canonical bindings, checker invocations, receipts, and identity accountability for SIM conformance."
readme = "README.md"
publish = true
repository.workspace = true
homepage.workspace = true

[package.metadata.sim-recipes]
policy = "descriptor"
reason = "The recipe is a machine-indexed description of an effect-free record contract."

[dependencies]
sha2 = "0.10"
sim-kernel = { version = "0.4.0" }

[lints]
workspace = true
