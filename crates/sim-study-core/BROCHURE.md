# Study facts that stay portable

`sim-study-core` gives every SIM domain the same small vocabulary for naming a
subject revision, a study coordinate, its outcomes, measurements, evidence,
decisions, and selections. Records have deterministic kernel `Datum` identities
and fail-closed privacy/export rules, without pulling in a database, statistics
engine, runtime, or AI stack.
