[package]
name = "sim-study-core"
version = "0.2.0"
edition = "2024"
license = "MPL-2.0"
description = "Pure canonical study identity, evidence, transition, and decision records for SIM."
readme = "README.md"
publish = true
repository = "https://github.com/sim-nest/sim-foundation"
homepage = "https://github.com/sim-nest/sim-foundation"

[package.metadata.sim-recipes]
policy = "rustdoc"
reason = "The pure record contract is taught through rustdoc and its exhaustive conformance test."

[dependencies]
sim-kernel = { version = "0.4.0" }

[lints]
workspace = true
