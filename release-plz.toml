# release-plz keeps workspace versions and dependency updates consistent.
# Publishable crates share one version set; dev harnesses are publish=false in
# their own Cargo.toml and are skipped automatically.
[workspace]
# One version for the workspace group; bump together.
dependencies_update = true
git_release_enable = true
# Publish crates in dependency order (release-plz computes the order).
